/**
 * Placeholder smoke test so `npm test` runs until feature specs are added.
 * Add real tests matching Jest testMatch (e.g. *.test.js under __tests__).
 */
describe('backend smoke', () => {
  it('runs the test suite', () => {
    expect(true).toBe(true);
  });
});
