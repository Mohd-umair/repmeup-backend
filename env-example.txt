# Copy this file to .env and fill in your actual values

# Environment
NODE_ENV=development
PORT=3000

# Super Admin (for scripts/createSuperAdmin.js and admin panel login)
# SUPER_ADMIN_EMAIL=admin@repmeup.com
# SUPER_ADMIN_PASSWORD=YourSecurePassword123
# SUPER_ADMIN_FIRST_NAME=Super
# SUPER_ADMIN_LAST_NAME=Admin
# Super Admin Panel APIs: /api/super-admin/* (JWT + role super_admin|admin). See docs/SUPER_ADMIN_API.md

# MongoDB
MONGODB_URI=mongodb://localhost:27017/orm_development

# Redis
REDIS_URL=redis://localhost:6379

# Worker concurrency (core worker.js — webhooks, AI, auto-reply, email, voice)
# WEBHOOK_CONCURRENCY=10
# AI_CONCURRENCY=10
# AUTOREPLY_CONCURRENCY=5
# ENABLE_CAMPAIGN_IN_CORE_WORKER=true   # local dev: campaigns in core worker; production: false + run campaignWorker.js

# WhatsApp campaign broadcast (campaignWorker.js / campaign-send queue)
# CAMPAIGN_BATCH_SIZE=100
# CAMPAIGN_BATCH_CONCURRENCY=20
# CAMPAIGN_SENDS_PER_SECOND=30
# CAMPAIGN_MAX_INFLIGHT_PER_WABA=5
# CAMPAIGN_MAX_SEND_ATTEMPTS=3
# CAMPAIGN_INBOX_CONCURRENCY=10
# CAMPAIGN_WABA_PAUSE_MS=120000
# CAMPAIGN_WABA_429_THRESHOLD=10
# CAMPAIGN_SKIP_STATUS_SOCKET=true
# INTERACTION_MAX_INLINE_REPLIES=500

# Rate limiting (optional; defaults: 1000 req/15 min per IP, or 10000 in development)
# RATE_LIMIT_MAX_REQUESTS=1000
# RATE_LIMIT_WINDOW_MS=900000
# To allow more requests: RATE_LIMIT_MAX_REQUESTS=5000
# To disable rate limiting entirely (e.g. all traffic behind one IP): RATE_LIMIT_DISABLED=true

# Strict brute-force limiter for /auth login, OTP, and password-reset endpoints
# (defaults: 10 attempts / 15 min per IP)
# AUTH_RATE_LIMIT_MAX=10
# AUTH_RATE_LIMIT_WINDOW_MS=900000

# Bull Board queue dashboard (/admin/queues) — REQUIRED to enable it.
# If either is unset the dashboard is disabled (returns 404) rather than served openly.
BULL_BOARD_USER=change-me
BULL_BOARD_PASSWORD=change-me-to-a-long-random-string

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production-minimum-32-characters
JWT_EXPIRE=7d
JWT_REFRESH_SECRET=your-refresh-token-secret-change-in-production
JWT_REFRESH_EXPIRE=30d

# Frontend
FRONTEND_URL=http://localhost:4200
# Comma-separate for super admin panel (port 4201): http://localhost:4200,http://localhost:4201
CORS_ORIGIN=http://localhost:4200

# Meta/Facebook/Instagram/WhatsApp
META_APP_ID=your_meta_app_id
META_APP_SECRET=your_meta_app_secret
FACEBOOK_APP_SECRET=your_meta_app_secret  # Same as META_APP_SECRET, used for data deletion
META_REDIRECT_URI=http://localhost:3000/api/platforms/meta/callback
META_WEBHOOK_VERIFY_TOKEN=your_webhook_verify_token

# WhatsApp Embedded Signup (“Connect WhatsApp” in the app)
# Public OAuth redirect Meta calls after the user finishes onboarding (must match Meta App > Facebook Login > Valid OAuth Redirect URIs)
# Example: https://api.yourdomain.com/api/platforms/whatsapp/callback
WHATSAPP_CALLBACK_URL=
# Configuration ID from Meta App Dashboard → WhatsApp → Embedded Signup (same as config_id in:
# https://business.facebook.com/messaging/whatsapp/onboard/?app_id=…&config_id=…)
META_WHATSAPP_CONFIG_ID=
# Optional: versioned Login dialog, e.g. v23.0 → https://www.facebook.com/v23.0/dialog/oauth
# META_WHATSAPP_OAUTH_DIALOG_VERSION=v23.0

# WhatsApp Business API
WHATSAPP_PHONE_NUMBER_ID=your_phone_number_id
WHATSAPP_BUSINESS_ACCOUNT_ID=your_business_account_id
WHATSAPP_ACCESS_TOKEN=your_whatsapp_access_token

# ── Interakt tech-partner (BSP) ──────────────────────────────────────────────
# Set these to onboard new WhatsApp numbers through Interakt instead of Meta direct.
# Existing Meta-direct connections keep working either way: the transport is chosen
# per connection from platformData.provider, so leaving these blank changes nothing.
#
# INTERAKT_ISV_TOKEN   the single partner access token Interakt issues you. Used as
#                      `Authorization: <token>` on api.interakt.ai and as
#                      `x-access-token` on the amped-express Graph proxy.
# INTERAKT_SOLUTION_ID the Partner Solution ID passed to Embedded Signup as
#                      extras.setup.solutionID. Presence of this value is what makes
#                      the frontend use the FB JS SDK flow.
INTERAKT_ISV_TOKEN=
INTERAKT_SOLUTION_ID=
INTERAKT_API_BASE=https://amped-express.interakt.ai/api
INTERAKT_PARTNER_API_BASE=https://api.interakt.ai/v1
INTERAKT_DATA_LOCALIZATION_REGION=IN
# Optional. Only needed if Interakt's console probes the partner webhook with a
# Meta-style hub.challenge handshake. Falls back to WHATSAPP_WEBHOOK_VERIFY_TOKEN.
# The POST webhook itself is authenticated by the isv_name_token Interakt echoes back.
INTERAKT_WEBHOOK_VERIFY_TOKEN=

# New support ticket alerts (POST /api/tickets) — email only
# Default recipient: umair9317@gmail.com (override with ADMIN_SUPPORT_TICKET_EMAIL)
# ADMIN_SUPPORT_TICKET_EMAIL=umair9317@gmail.com

# Google/YouTube
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3000/api/platforms/google/callback

# LinkedIn
LINKEDIN_CLIENT_ID=your_linkedin_client_id
LINKEDIN_CLIENT_SECRET=your_linkedin_client_secret
LINKEDIN_REDIRECT_URI=http://localhost:3000/api/auth/linkedin/callback
# Repmeup use case: organizations post on their LinkedIn Company Page. That requires Community Management API approval.
# Set to 'true' once LinkedIn approves Community Management API (org posting/comments).
LINKEDIN_ENABLE_ADVANCED_SCOPES=true
# Only set to 'true' as a temporary workaround: request only w_member_social so "Connect LinkedIn" doesn't error.
# With this on, Company Page will NOT be available—orgs cannot post as their page until you set this back to false and have org approval.
# LINKEDIN_MEMBER_SOCIAL_ONLY=true
#
# Member inbox (list YOUR posts + read comments): posting uses w_member_social; READ APIs need r_member_social.
# Add r_member_social to your LinkedIn app (Products/scopes) if LinkedIn allows, then:
# LINKEDIN_INCLUDE_R_MEMBER_SOCIAL=true
# Reconnect LinkedIn. If OAuth errors on scope, request r_member_social access from LinkedIn Developer Support.
#
# If publish returns 403 partnerApiPostsExternal.CREATE and you connected a Company Page:
# - For Page posting: you need Community Management API approval from LinkedIn (no code fix).
# - To post as the person's profile instead (while still having a page on the connection), set:
# LINKEDIN_POST_AS_PERSON=true
# Then restart API and reconnect LinkedIn so the token includes w_member_social.
# Webhook configuration (requires special LinkedIn API access)
LINKEDIN_WEBHOOK_VERIFY_TOKEN=your_secure_random_token_here
LINKEDIN_WEBHOOK_SECRET=your_webhook_signing_secret_here

# AI (OpenAI only — chat, sentiment, inbox replies, content generation)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# OPENAI_MODEL=gpt-4
# Newer models (use exact API ids, lowercase — e.g. not "GPT-5.3"):
# OPENAI_MODEL=gpt-5.4
# OPENAI_MODEL=gpt-5.2
# OPENAI_MODEL=gpt-4o
# Image generation model (OpenAI Image API):
# OPENAI_IMAGE_MODEL=gpt-image-1.5
# Optional: retries for transient errors (aborted, timeouts, 429/502/503); default 3
# OPENAI_IMAGE_MAX_RETRIES=3
# Optional: per-request timeout ms; default 120000 (2 min)
# OPENAI_IMAGE_TIMEOUT_MS=120000

# Email — GoDaddy Workspace / Professional Email (domain + email on GoDaddy, hosting on Hostinger)
# GoDaddy outgoing SMTP server: smtpout.secureserver.net
# Port 465 = SSL (recommended) | 587 = STARTTLS
# SMTP_USER = full GoDaddy email address (e.g. info@repmeup.in)
# SMTP_PASS = the mailbox password (same one you use to log into GoDaddy webmail)
# Password with # $ ! chars → wrap in single quotes: SMTP_PASS='pass#here'
# If auth still fails: log into GoDaddy webmail first with same user/pass to verify credentials work.
# Verbose SMTP trace on server: SMTP_DEBUG=true
SMTP_HOST=smtpout.secureserver.net
SMTP_PORT=465
SMTP_USER=info@repmeup.in
SMTP_PASS=your_godaddy_mailbox_password
FROM_EMAIL=info@repmeup.in
FROM_NAME=RepMeUp
# SMTP_AUTH_METHOD=LOGIN
# SMTP_DEBUG=true

# Rate limiting (optional; defaults: 1000 req/15 min per IP, or 10000 in development)
# RATE_LIMIT_MAX_REQUESTS=5000
# RATE_LIMIT_WINDOW_MS=900000
# To disable rate limiting (e.g. all users behind one IP): RATE_LIMIT_DISABLED=true

# File Upload
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,image/webp

# Logging
LOG_LEVEL=debug

# Demo booking alert recipient (defaults to info@repmeup.in)
DEMO_BOOKING_EMAIL=info@repmeup.in
